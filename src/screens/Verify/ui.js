import { Box, styled } from "@mui/material";

export const VerifyWrapper = styled(Box)({
    flex: 1,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
});

export const VerifyFormContainer = styled(Box)({
    width: '400px',
    minHeight: '200px',
    boxShadow: "rgba(0, 0, 0, 0.16) 0px 1px 4px",
    borderRadius: '0.8em',
    padding: '2em 1em',
    border: '1px solid #d9d9d9'
});
